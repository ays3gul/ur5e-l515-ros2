arm_control:
  ros__parameters:
    # Groups of 10: [dim_x, dim_y, dim_z, pos_x, pos_y, pos_z, ori_x, ori_y, ori_z, ori_w]
    # Ground/table slab under the robot base to prevent planning into the floor.
    obstacles:
      - 2.0
      - 2.0
      - 0.02
      - 0.0
      - 0.0
      - -0.01
      - 0.0
      - 0.0
      - 0.0
      - 1.0
